name: F# Cluster Smoke

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Smoke mode to run through the F# control plane
        required: true
        default: sounio
        type: choice
        options:
          - sounio
          - benchmark
      path:
        description: Full path rebuild or fast path reusing bundle/image/import
        required: true
        default: fast
        type: choice
        options:
          - fast
          - full

permissions:
  contents: read

concurrency:
  group: fsharp-cluster-smoke
  cancel-in-progress: false

jobs:
  cluster-smoke:
    runs-on:
      - self-hosted
      - macOS
    timeout-minutes: 90
    env:
      DARWIN_RESEARCH_OS_SSH_HOST: ${{ vars.DARWIN_RESEARCH_OS_SSH_HOST }}
      DARWIN_RESEARCH_OS_WORKER_IMAGE: ${{ vars.DARWIN_RESEARCH_OS_WORKER_IMAGE }}
      SOUNIO_SOURCE_ROOT: ${{ vars.SOUNIO_SOURCE_ROOT }}
      SOUNIO_BINARY_ROOT: ${{ vars.SOUNIO_BINARY_ROOT }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare local runner env
        shell: bash
        run: |
          dotnet_root="${DOTNET_ROOT:-$HOME/.dotnet}"
          source_root="${SOUNIO_SOURCE_ROOT:-$HOME/sounio-lang-sounio}"
          binary_root="${SOUNIO_BINARY_ROOT:-$source_root}"
          ssh_host="${DARWIN_RESEARCH_OS_SSH_HOST:-demetrios@DEVdesktop}"
          worker_image="${DARWIN_RESEARCH_OS_WORKER_IMAGE:-darwin-research-os-worker:k3s-dev}"

          echo "DOTNET_ROOT=${dotnet_root}" >> "$GITHUB_ENV"
          echo "PATH=${dotnet_root}:$PATH" >> "$GITHUB_ENV"
          echo "SOUNIO_SOURCE_ROOT=${source_root}" >> "$GITHUB_ENV"
          echo "SOUNIO_BINARY_ROOT=${binary_root}" >> "$GITHUB_ENV"
          echo "DARWIN_RESEARCH_OS_SSH_HOST=${ssh_host}" >> "$GITHUB_ENV"
          echo "DARWIN_RESEARCH_OS_WORKER_IMAGE=${worker_image}" >> "$GITHUB_ENV"

      - name: Resolve smoke target
        id: target
        shell: bash
        run: |
          case "${{ inputs.mode }}:${{ inputs.path }}" in
            sounio:fast) target="smoke-cluster-sounio-fast" ;;
            sounio:full) target="smoke-cluster-sounio" ;;
            benchmark:fast) target="smoke-cluster-benchmark-fast" ;;
            benchmark:full) target="smoke-cluster-benchmark" ;;
            *)
              echo "Unsupported smoke combination: ${{ inputs.mode }} / ${{ inputs.path }}" >&2
              exit 1
              ;;
          esac
          echo "target=${target}" >> "$GITHUB_OUTPUT"

      - name: Show runner toolchain
        shell: bash
        run: |
          python3 --version
          dotnet --info
          docker version
          make help

      - name: Run cluster smoke
        shell: bash
        run: make "${{ steps.target.outputs.target }}"

      - name: Upload smoke logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fsharp-cluster-smoke-${{ inputs.mode }}-${{ inputs.path }}-${{ github.run_id }}
          path: |
            .codex-run/**
            .fsharp-cluster-smoke-objects/**
          if-no-files-found: warn
          retention-days: 7
          include-hidden-files: true

[package]
name = "monte_carlo"
version = "0.1.0"
authors = ["Sounio Team"]
description = "Monte Carlo simulations: Pi estimation and option pricing"
license = "MIT"
edition = "2024"

[dependencies]
std = { path = "../../stdlib" }

[features]
default = ["prob"]
prob = []       # Enable probabilistic primitives

[profile.release]
optimization = 3
lto = true

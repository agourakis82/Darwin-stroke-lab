[package]
name = "data_pipeline"
version = "0.1.0"
authors = ["Sounio Team"]
description = "Data processing pipeline: CSV to JSON with async/channels"
license = "MIT"
edition = "2024"

[dependencies]
std = { path = "../../stdlib" }

[features]
default = ["async"]
async = []      # Enable async processing

[profile.release]
optimization = 3
